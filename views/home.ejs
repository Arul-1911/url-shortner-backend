<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL SHORTNER</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <style>
      body {
          background-color: #fff;
          color: #000;
      }

      .container {
          margin-top: 20px;
      }

      h1 {
          color: #000;
      }

      .input-group {
          margin-bottom: 20px;
      }

      .card {
          border: 1px solid #000;
          margin-bottom: 20px;
          border-radius: 20px;
          height: 90%;
          margin-top: 8%;
      }

      .card-header {
          background-color: #000;
          color: #fff;
      }

      .btn-outline-secondary {
          color: #000;
          border-color: #000;
      }

      .btn-outline-secondary:hover {
          background-color: #000;
          color: #fff;
      }

      a {
          color: #000;
          text-decoration: none;
      }

      a:hover {
          color: #000;
          text-decoration: underline;
      }

      .btn-danger {
          background-color: black;
          color: #fff;
          border-color: black;
          text-decoration: none;
      }

      .btn-danger:hover {
          background-color: #bd2130;
          border-color: #bd2130;
      }
  </style>
</head>
<body>
   <div class="container">
    <div class="row">
        <div class="col-lg-12 text-center">
        </div>
    </div>
    <form action="/create" method="post">
    <div class="row">
    
        <div class="input-group mb-4">
            <input type="text" class="form-control" name="longurl"  placeholder="Paste your Url" aria-label="Url"  required>
            <input type="submit" class="btn btn-outline-secondary" value="submit">
          </div>
   
      </div>
    </form>
      <div class="row">
        <% for (let i=0;i< urlResult.length; i++) { %>
        <div class="col-lg-3 col-sm-11 col-md-6">
            <div class="card border-secondary mb-3 p-2" style="max-width: 18rem;">
                <div class="card-header">Total clicks: <%= urlResult[i].clickCount %></div>
                <div class="card-body text-secondary">
                  <p>Shortened Url:</p>
                  <h6 class="card-title">
                    <a href="https://url-shortner-6uhu.onrender.com/<%= urlResult[i].shortUrl %>" target="_blank">https://url-shortner-6uhu.onrender.com/<%= urlResult[i].shortUrl %></a>
                  </h6>
                  <hr>
                  <p>Long Url:</p>
                  <p class="card-text"><%= urlResult[i].longUrl %></p>
                  <a href="/delete/<%=urlResult[i].id %>" class="btn btn-sm btn-danger">Remove</a>
                </div>
              </div>
      </div>
      <% } %>
   </div>
</body>
</html>